// import React, { useContext } from 'react';
// import { PostImageContext } from '../Context/ImageProvider';  // Import the context

// const PostedImageDisplay = () => {
//     const { postedImage } = useContext(PostImageContext);  // Get the posted image from context

//     return (
//         <div className="posted-image-container">
//             {postedImage ? (
//                 <img src={postedImage} alt="Posted Image" style={{ maxWidth: '100%' }} />
//             ) : (
//                 <p>No image posted yet.</p>
//             )}
//         </div>
//     );
// };

// export default PostedImageDisplay;
